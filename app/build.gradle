apply plugin: 'com.android.application'

android {
    compileSdkVersion 13
    buildToolsVersion "22.0.1"

    defaultConfig {
        applicationId "cat.udl.eps.fragments.ejbase"
        minSdkVersion 11
        targetSdkVersion 11
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}
